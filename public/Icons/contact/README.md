Place your icon files here for contact details:

- email.svg — recommended: 24x24, stroke icons or simple filled SVG
- phone.svg — recommended: 24x24

Use the same filenames so the site picks them up without code changes.

If you want PNGs, update the `src` paths in `src/app/contact/page.tsx` to point to the correct extension.